<Window x:Class="WpfApp.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:" Grid.Row="0" VerticalAlignment="Center" />
        <TextBox x:Name="UsernameTextBox" Grid.Row="0" Margin="100,0,0,0" Width="200"/>

        <TextBlock Text="Password:" Grid.Row="1" VerticalAlignment="Center" Margin="0,10,0,0"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="100,10,0,0" Width="200"/>

        <Button Content="Login" Grid.Row="2" Width="100" Height="30" Margin="0,20,0,0"
                HorizontalAlignment="Left" Click="LoginButton_Click"/>

        <TextBlock x:Name="StatusTextBlock" Grid.Row="3" Margin="0,20,0,0" Foreground="Red"/>
    </Grid>
</Window>

