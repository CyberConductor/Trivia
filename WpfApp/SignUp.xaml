<Window x:Class="WpfApp1.SignUp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Sign Up" Height="450" Width="800" Background="#f2f2f2" WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <!-- כפתור חזור -->
        <Button Click="Back_Click"
        Width="40" Height="40"
        HorizontalAlignment="Right"
        VerticalAlignment="Top"
        Background="Transparent"
        BorderBrush="Transparent"
        Cursor="Hand"
        ToolTip="Back">
            <materialDesign:PackIcon Kind="ArrowBack"
                             Width="24" Height="24"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center">
                <materialDesign:PackIcon.RenderTransform>
                    <ScaleTransform ScaleX="-1" />
                </materialDesign:PackIcon.RenderTransform>
            </materialDesign:PackIcon>
        </Button>
        <!-- טופס הרשמה -->
        <Border Background="White" CornerRadius="10" Padding="30" Width="350" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <StackPanel>
                <TextBlock Text="Create Account" FontSize="24" FontWeight="Bold" Foreground="#333" Margin="0 0 0 20" HorizontalAlignment="Center"/>

                <TextBox 
                    x:Name="UsernameBox"
                    Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                    materialDesign:HintAssist.Hint="Username"
                    Margin="0 8 0 10"
                    FontSize="14"
                    BorderThickness="2"
                    BorderBrush="LightGray"
                    Padding="8"
                />

                <PasswordBox 
                    x:Name="PasswordBox"
                    Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                    materialDesign:HintAssist.Hint="Password"
                    Margin="0 10"
                    FontSize="14"
                    BorderThickness="2"
                    BorderBrush="LightGray"
                    Padding="8"
                />

                <TextBox x:Name="EmailBox"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Email"
                     Margin="0 10"
                     FontSize="14"
                     BorderThickness="2"
                     BorderBrush="LightGray"
                     Padding="8" 
                />

                <Button 
                    Content="Sign Up"
                    Click="SignUp_Click"
                    Margin="0 20 0 0"
                    Padding="10"
                    Background="SteelBlue"
                    Foreground="White"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Cursor="Hand"
                />
            </StackPanel>
        </Border>
    </Grid>
</Window>
